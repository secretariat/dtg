<%= form_for(@edizm) do |f| %>
  <% if @edizm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@edizm.errors.count, "error") %> prohibited this edizm from being saved:</h2>

      <ul>
      <% @edizm.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= link_to 'Назад', edizms_path, :class => 'btn btn-info' %>
    <%= f.submit :'Сохранить', :class => 'btn btn-info'%>
  </div>
<% end %>
